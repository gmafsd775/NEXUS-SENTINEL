[phases]
  [phases.install]
    nixPkgs = ["libGL"]  # Add system library support if needed
    cmds = [
      "pip install -r requirements.txt",
      "python -m pip uninstall -y opencv-python"
    ]
